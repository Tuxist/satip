class MediaBackend {
public:
  MediaBackend();
  ~MediaBackend();
  
  void mute();
  void unmute();
  void louder();
  void lower();
  
  void play(void *data);
  void pause();
  void stop();
private:
  void *_Data;
};
